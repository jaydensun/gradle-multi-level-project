subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.5
    version = '1.0'

    repositories {
        maven { url property('mavenUrl') }
    }

    dependencies {
        compile "org.slf4j:slf4j-api:${slf4jVersion}"
    }
}